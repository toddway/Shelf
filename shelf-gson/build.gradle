apply plugin: 'java'

dependencies {
    compile 'com.google.code.gson:gson:2.3.1'
    compile project(':shelf')
    testCompile 'junit:junit:4.12'
}

jar {
    from sourceSets.main.allSource //this adds source to the compiled jar
}